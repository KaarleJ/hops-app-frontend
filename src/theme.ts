import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0B0754',
    },
    secondary: {
      main: '#126CDB',
    },
    text: {
      primary: '#6D6C6C',
    },
    error: {
      main: '#C01D1D',
    },
  },
});

export default theme;
