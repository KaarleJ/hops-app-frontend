import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0B0754'
    },
    secondary: {
      main: '#313135'
    },
    text: {
      primary: '#6D6C6C'
    },
  }
});

export default theme;